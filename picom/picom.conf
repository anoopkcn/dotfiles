backend = "glx";
vsync = true;

shadow = false;
# shadow-radius = 12;
# shadow-offset-x = -8;
# shadow-offset-y = -8;
# shadow-opacity = 0.35;

corner-radius = 0;
# rounded-corners-exclude = [
#   "window_type = 'dock'",
#   "window_type = 'desktop'",
#   "class_g = 'Dunst'",
#   "class_g = 'dmenu'"
# ];

fading = false;
# fade-in-step = 0.04;
# fade-out-step = 0.04;

# inactive-opacity = 0.90;
# active-opacity = 1.0;
# frame-opacity = 1.0;

# opacity-rule = [
#   "100:class_g = 'kitty'",
#   "100:class_g = 'firefox'",
#   "100:class_g = 'zen'"
# ];
