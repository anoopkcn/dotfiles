# ~/.tmuxinator/mhpc.yml

name: mhpc
root: ~/Dropbox/Workbench/MHPC
# <%= @args[0] %>

windows:
  - local:
      layout: main-vertical
      panes:
        - clear
  - bash:
      layout: main-vertical
      panes:
        - clear
