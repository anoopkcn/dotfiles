setopt extended_glob null_glob
path=(
    /Users/akc/.local/bin
    /Library/TeX/texbin
    $path
)
typeset -U path
path=($^path(N-/))
export PATH
export EDITOR='nvim'
export VIRTUAL_ENV_DISABLE_PROMPT=1
export CONDA_CHANGEPS1=no

bindkey -e

HISTFILE=$HOME/.zsh_history
HISTSIZE=10000
SAVEHIST=$HISTSIZE
setopt HIST_IGNORE_DUPS
setopt HIST_IGNORE_SPACE
setopt SHARE_HISTORY

source_file() {  [ -f "$1" ] && source "$1"  }
path() { echo -e ${PATH//:/\\n} }

source_file $HOME/.privaterc
source_file "${ZDOTDIR:-$HOME/.config/zsh}/plugin-manager.sh"
source_file "${ZDOTDIR:-$HOME/.config/zsh}/minimal-prompt.sh"
source_file "${ZDOTDIR:-$HOME/.config/zsh}/fzf.sh"

alias ls="ls --color=auto"
alias duh="du -h -d 0 [^.]*"
alias sync="rsync -macviz --exclude-from=${ZDOTDIR:-$HOME/.config/zsh}/rsyncignore"
alias notes="cd ~/Dropbox/notes"
alias ls='eza --hyperlink'
alias rg="rg --hyperlink-format=kitty"
alias gs="git status"
alias vim='nvim'

plugin add "zshzoo/macos"
plugin add "zsh-users/zsh-syntax-highlighting"
plugin add "anoopkcn/zig-shell-completions"

autoload -Uz compinit && compinit
if [ -n "$ZSH_VERSION" ]; then
    zstyle ':completion:*' matcher-list 'm:{a-z}={A-Za-z}'

fi

if [ -n "$BASH_VERSION" ] || [ -n "$ZSH_VERSION" ]; then
    if command -v brew >/dev/null 2>&1; then
        eval "$(brew shellenv)"
    fi
fi


autoload -U edit-command-line
zle -N edit-command-line
bindkey '^Xe' edit-command-line
export BAT_THEME="OneHalfDark"
source <(fzf --zsh)
eval "$(zoxide init zsh)"

source "$HOME/.cargo/env" 
